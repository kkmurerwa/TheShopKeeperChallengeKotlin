package org.example

fun myprintln(message: String? = null) {
    println("=========================================================================================================")
    if (message != null) println(message)
}